//- Panel mixin
mixin panel(heading, style)
  - var style = (typeof style === 'undefined') ? "default" : style
  div( class=["panel", "panel-" + style] )&attributes(attributes)
    .panel-heading= heading
    .panel-body
      block
