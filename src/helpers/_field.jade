mixin field(options)
  .field(class='field' + options.type ? options.type : "input")
    label #{options.text}:
      input(
        type=options.type || "text",
        name=options.name,
        value=options.value
      )
